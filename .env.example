# ---- Resend (order emails) — required ----
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
TO_EMAIL=you@example.com
FROM_EMAIL=onboarding@resend.dev

# ---- Razorpay (add later) ----
# RAZORPAY_KEY_ID=
# RAZORPAY_KEY_SECRET=

# ---- Meta Pixel (add later, this one is client-side, no env var needed) ----
# Just paste your Pixel ID into the commented block in index.html <head>
